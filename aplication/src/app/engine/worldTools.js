export default class world {

    //OBJECT CLASS FOR WORLD

    constructor(name, init, end) {

        this.name = name,
            this.init = init,
            this.end = end,
            this.models = [],
            this.conections = [],
            this.lists = []
    }
}